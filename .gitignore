# Java / Maven
target/
*.class
*.jar
*.war
.mvn/wrapper/maven-wrapper.jar
dependency-reduced-pom.xml

# Node / Vite
node_modules/
dist/
.vite/
*.tsbuildinfo

# Env — .env is intentionally tracked (private, solo repo). Local override
# variants stay ignored so a stray .env.local can't get committed by accident.
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.iml
*.iws
*.ipr

# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
logs/
tmp/
